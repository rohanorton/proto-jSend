/*jslint node:true indent:2 */
/*globals describe, it */
'use strict';

var assert = require('assert');

describe('jSend', function () {
  it('should exist', function () {
    assert(jSend);
  });
});
