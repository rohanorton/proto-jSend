{
  "name": "proto-jsend",
  "version": "0.0.0",
  "description": "jSend middleware for express",
  "main": "index.js",
  "scripts": {
    "test": "jasmine",
    "lint": "jslint *.js **/*.js",
    "test:watch": "npm test -s; onchange '*.js' '**/*.js' -- npm test -s",
    "lint:watch": "npm run lint -s; onchange '*.js' '**/*.js' -- npm run lint -s"
  },
  "author": "Rohan Orton",
  "license": "MIT",
  "devDependencies": {
    "jasmine": "^2.3.1",
    "jslint": "^0.9.1",
    "node-mocks-http": "git+https://github.com/Abe404/node-mocks-http.git",
    "onchange": "^1.1.0"
  },
  "dependencies": {
    "arg-err": "0.1.0"
  }
}
